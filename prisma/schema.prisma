generator client {
  provider = "prisma-client-js"
  // output   = "../../node_modules/.prisma/client"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://mexhi:qQ&hh1_M@db.sunthewhat.com:15432/mentalexhi"
}

model workshopBooking {
  id        Int    @id @default(autoincrement())
  title     String @unique
  userCount Int
}
